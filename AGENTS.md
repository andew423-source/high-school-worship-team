# 찬양팀 운영 사이트

## 목표

조 편성, 토요모임 출결, 주일 등단 배정과 등단표 생성을 관리하는 내부 사이트다.

## 핵심 규칙

- 모든 자동 배정은 관리자가 수정하고 확정할 수 있어야 한다.
- 조건 충돌이나 후보 부족 시 억지로 배정하지 말고 원인을 표시한다.
- 학생은 전날 토요모임에 출석하고 자신의 예배 부서와 일치할 때만 등단 후보가 된다.
- 학생 인도자는 인도자로 선택되면 싱어·콰이어 후보에서 제외하되 등단 횟수에는 포함한다.
- 싱어 스탭은 싱어 또는 직접 선택된 인도자로만 배정한다.
- 스탭의 1·2부 중복 등단은 인원 부족 시에만 허용한다.
- 학생의 전체 등단 및 싱어 횟수를 고르게 배분하고 연속 등단을 최소화한다.
- 등단표는 인도자를 중앙에 두고 싱어와 콰이어를 좌우 균형 있게 배치한다.

## 구현 원칙

- 배정 규칙은 UI와 분리된 테스트 가능한 도메인 로직으로 구현한다.
- 자동 배정 결과에는 적용 조건과 예외 이유를 표시한다.
- 원본 데이터와 수동 수정·확정 이력을 보존한다.
- 출결 화면은 모바일 사용을 우선한다.
- 개인정보를 로그, 테스트 데이터, 화면에 불필요하게 노출하지 않는다.
- 사업 규칙을 바꾸는 모호성이 있을 때만 사용자에게 확인한다.

## 작업 및 검증

- 기존 구조와 패턴을 먼저 확인하고 필요한 범위만 수정한다.
- `staff-guide-site/`를 변경한 경우 그 디렉터리에서 `npm run lint`와 `npm test`를 실행한다.
- DB 스키마 변경 시 `npm run db:generate` 결과를 함께 검토한다.
- 검증을 실행하지 못했다면 이유와 미검증 항목을 보고한다.

상세 제품 요구사항은 `docs/product-spec.md`를 따른다.
